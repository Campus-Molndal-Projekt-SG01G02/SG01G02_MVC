@{
    ViewData["Title"] = "Staff Dashboard";
}

<h2>Staff Dashboard</h2>

@if (ViewBag.Message != null)
{
    <div class="alert alert-info">
        @ViewBag.Message
    </div>
}

<!-- TODO: Render orders (read-only) here -->