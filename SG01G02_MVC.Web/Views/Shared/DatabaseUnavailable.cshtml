@{
    ViewData["Title"] = "Database Unavailable";
}

<h1>Database Connection Error</h1>
<p>
    The application could not connect to the database.
    This may occur in CI/CD environments or local setups where SQLite isn't configured.
</p>
<p>
    Please ensure the SQLite file exists and is properly configured in <code>appsettings.json</code>.
</p>